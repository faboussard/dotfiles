- name: Bootstrap development environment
  hosts: localhost

  tasks:
  - name: pull from git
    become: yes
    git:
      repo: git@github.com:faboussard/dotfiles.git
      dest: ~/Dotfiles
      update: yes
      version: master
